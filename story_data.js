/* STORY DATA CONFIGURATION
   ========================
   Формат этапа (Stage Object):
   {
       id: 1,                  // Номер этапа (должен идти по порядку)
       waitJumps: 2,           // Сколько прыжков нужно сделать ПОСЛЕ предыдущего этапа, чтобы запустить этот
       
       // Условия (необязательно)
       reqItems: [             // Требуемые предметы для запуска диалога успеха
           { id: 'c20', count: 2 }, 
           { id: 'c23', count: 2 }
       ],
       
       // NPC для сцены
       npcs: [
           { 
               name: "Red",       // Имя (используется в диалогах)
               color: "#ff1744",  // Цвет корабля
               startSide: "left"  // Откуда вылетает (left, right, top, bottom, random)
           },
           { name: "Blue", color: "#2979ff", startSide: "right" }
       ],

       // Диалоги (массив объектов)
       // Если есть reqItems, то диалог делится на success (есть предметы) и fail (нет предметов)
       dialogue: [
           { name: "Red", text: "Привет! Я тут мимо пролетал." },
           { name: "Blue", text: "А я тебя искал." }
       ],
       
       // Если есть предметы, используем такую структуру:
       // dialogue: {
       //    start: [ ... ], // Вводная часть (если нужна)
       //    success: [ ... ], // Если предметы есть (они изымутся автоматически)
       //    fail: [ ... ]     // Если предметов нет
       // }
       
       reward: 100 // Кредиты за выполнение (только при success или обычном диалоге)
   }
*/

